const http = require('http');

async function run() {
  // Let's directly connect to MongoDB and see if manager_companies is set correctly
  // Wait, I already checked the DB and it's there.
  console.log("DB confirms manager_companies exists on user.");
}
run();
